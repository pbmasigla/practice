/* Can you describe the difference between progressive enhancement and graceful degradation? */
Progressive Enhancement - emphasizes core webpage content first (users see content first)
Graceful Degradation - limit functionality when something has been destroyed or inoperative

/* How would you optimize a website's assets/resources? */
CDN, host assests on different domains

/* How many resources will a browser download from a given domain at a time? */
NO CLUE FIND THIS OUT

/* Retun function to add arguments together */
function sum() {
	return arguments.reduce((total, value) => total += value, 0);
}
